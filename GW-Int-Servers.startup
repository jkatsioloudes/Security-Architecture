# Gateway Internal Servers is responsible to 
# connect internal users to the Internal DNS
# and WWW Servers.

ifconfig eth0 10.0.8.2/30
ifconfig eth1 10.0.9.1/29

# Defaults to the Internal Firewall.
route add default gw 10.0.8.1
